<!DOCTYPE html>
<html>
  <head>
    <title>admin - Milk</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-terminal-window">
    <header>
      <%= render partial: "admin/partials/admin_navbar" %>
    </header>
      <p class="notice text-sky-500 px-4"><%= notice %></p>
      <p class="alert text-red-500 px-4"><%= alert %></p>
      <div class="container mx-auto min-h-screen px-4 md:px-0 mt-2 flex">
        <%= yield %>
      </div>
      
  </body>
</html>
