<!DOCTYPE html>
<html>
  <head>
    <title>Milk</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="">

      <nav class="flex justify-between items-center p-4 h-20 sticky top-0 bg-terminal-window-100">
        <div class="text-terminal-green-100">logo</div>
        <div class="flex space-x-2">
          <div class="text-terminal-green-100">email</div>
          <div class="text-terminal-green-100 md:hidden">dropdown</div>
        </div>
      </nav>

      <div class="flex">

        <main class="w-5/6 p-4 flex-grow">
          <% (1..100).each do |i| %>
            <p><%= i %></p>
          <%end%>
          <%= yield %>
        </main>
        <nav class="hidden md:flex flex-col justify-between w-1/6 p-4 text-center bg-terminal-window-100 sticky top-20 h-[calc(100vh-80px)]">
          <div class="text-terminal-green-100">
            sidebar top
          </div>
          <div class="text-terminal-green-100">
            sidebar bottom
          </div>
        </nav>

      </div>
      
  </body>
</html>
